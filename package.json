{
  "name": "fantasy-lol",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Fantasy-Liga fuer LoL-Esports: Kader aus echten Profispielern, Punkte aus echten Spielwerten.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply fantasy-lol --local",
    "db:remote": "wrangler d1 migrations apply fantasy-lol --remote",
    "test": "node test/run.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
